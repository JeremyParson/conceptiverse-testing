{{{code}}}

const testCases = {{{nested_array parameters}}};
const getSolutions = {{{arrayify expectedOutputs}}};

for (let i = 0; i < testCases.length; i++) {
    const testCase = testCases[i];
    const solution = getSolutions[i];
    const output = {{methodName}}{{{anonymous_invoke parameterCount}}};
    let outputString = '';
    let solutionString = '';
    if (output instanceof Array) {
        outputString = "[" + output.toString() + "]";
    } else {
        outputString = output.toString();
    }

    if (solution instanceof Array) {
        solutionString = "[" + solution.toString() + "]";
    } else {
        solutionString = solution.toString();
    }

    if (outputString === solutionString) {
        console.log(`Passed|(${testCase})|${solutionString}|${outputString}`);
        
    } else {
        console.log(`Failed|(${testCase})|${solutionString}|${outputString}`);
    }
}
